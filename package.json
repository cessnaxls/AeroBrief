{
  "name": "aerobrief-ipad",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "iPad-first flight planning and briefing PWA for flight simulation",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  }
}
